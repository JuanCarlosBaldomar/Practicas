#include <iostream>

using namespace std;

int main() {
    // Declaracion de variables
    float notaFinal;

    // Entrada de datos
    cout << "Ingrese la nota final del estudiante: ";
    cin >> notaFinal;

    // Condicion y salida de datos
    if (notaFinal >= 51) {
        cout << "Aprobado" << endl;
    } else {
        cout << "Reprobado" << endl;
    }

    return 0;
}
